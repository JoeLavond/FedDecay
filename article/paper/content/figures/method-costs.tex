\begin{figure*}[htbp]
    \centering
    \includegraphics[width=.9\linewidth]{images/run_costs.png}
    \caption{
        Total bytes communicated (\textit{top}) and floating point operations (\textit{bottom}) for each method's top-performing run. FedDecay produces similar costs to other single-model-based techniques. However, FedEM and pFedMe acquire much more significant costs.
    }
    %\vspace{-.5cm} % to avoid whitespace going to next page
    \label{fig: costs}
\end{figure*}
